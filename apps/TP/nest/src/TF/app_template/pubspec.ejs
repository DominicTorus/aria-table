name: <%= data1 %>
description: ""
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
   <%_ if (data.length > 0) { _%>
  <%_ data.forEach(function(pkg) { _%>
  <%= pkg.name %>: <% if (!pkg.rootUri) { %>^<%= pkg.pkgVersion %><% } else { %>
    path: <%= pkg.rootUri %>
      <% } %>
    <%_ }); _%>
  <%_ } _%>

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - android/sys/assets/png/logo.png
    - android/sys/assets/png/gen.png
    - .env

platforms:
  android: